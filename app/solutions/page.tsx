import Link from 'next/link'
const items = [{ href: '/solutions/automations', title: 'AI automations', copy: 'Remove repetitive work and let your tools move information for you.' }, { href: '/solutions/agents', title: 'AI agents', copy: 'Give customers and teams an always-on partner that can actually get things done.' }, { href: '/solutions/workflows', title: 'AI workflows', copy: 'Design reliable multi-step systems that turn a trigger into a measurable outcome.' }]
export default function SolutionsPage() { return <main className="site-shell min-h-screen px-6 py-28"><div className="mx-auto max-w-6xl"><p className="eyebrow">The Emporri system</p><h1 className="display-title mt-5 max-w-4xl">AI that does more than answer. It moves your business forward.</h1><div className="mt-16 grid gap-5 md:grid-cols-3">{items.map((item) => <Link href={item.href} key={item.href} className="glass-panel group p-7"><h2 className="text-2xl font-semibold text-white">{item.title}</h2><p className="mt-4 leading-7 text-white/60">{item.copy}</p><span className="mt-10 inline-block text-sm text-cyan-300 group-hover:text-pink-300">Explore solution →</span></Link>)}</div></div></main> }
